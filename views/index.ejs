<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Weather Checker</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Link to your CSS file in /public/css/style.css (optional) -->
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>🌦️ Weather Checker</h1>

  <!-- 
    Form: user enters a city name.
    Method = GET so the query string looks like /weather?city=Johannesburg
    Action = /weather (handled in routes.js)
  -->
  <form method="GET" action="/weather">
    <input type="text" name="city" placeholder="Enter city (e.g., Johannesburg)" required>
    <button type="submit">Check Weather</button>
  </form>

  <hr>

  <!-- 
    If forecast data exists (passed from routes.js), display it here.
    forecast.main.temp → temperature
    forecast.weather[0].description → weather description
  -->
  <% if (forecast) { %>
    <h2>Weather for <%= city%> : <%=forecast.dt_txt %></h2>
    <p><strong>Temperature:</strong> <%= forecast.main.temp %> °C</p>
    <p><strong>Condition:</strong> <%= forecast.weather[0].description %></p>
    <p><strong>Feels like:</strong> <%= forecast.main.feels_like %> °C</p>
    <p><strong>Humidity:</strong> <%= forecast.main.humidity %>%</p>
  <% } %>

  <!-- 
    If an error message exists (e.g., invalid city, API error),
    display it in red text.
  -->
  <% if (error) { %>
    <p style="color: red;"><%= error %></p>
  <% } %>
</body>
</html>